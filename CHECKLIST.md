Get the Data (ComponentDidMount) X

Inital state X

SetState X

Changed

<option key={course.id} className="item" value={course.id}>

To

<option key={course.id} className="item" value={course.name}>
